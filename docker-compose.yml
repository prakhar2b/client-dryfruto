services:
  # MongoDB Database
  mongodb:
    image: mongo:6.0
    container_name: dryfruto-mongodb2
    restart: always
    volumes:
      - mongodb_data:/data/db
    networks:
      - dryfruto-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27018/test --quiet
      interval: 10s
      timeout: 5s
      retries: 5

  # FastAPI Backend
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: dryfruto-backend2
    restart: always
    environment:
      - MONGO_URL=mongodb://mongodb:27018
      - DB_NAME=dryfruto2
    volumes:
      - uploads_data:/app/uploads
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - dryfruto-network

  # Nginx - Serves frontend and proxies API
  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    container_name: dryfruto-nginx2
    restart: always
    ports:
      - "8081:80"
    volumes:
      - uploads_data:/var/www/uploads:ro
    depends_on:
      - backend
    networks:
      - dryfruto-network

volumes:
  mongodb_data:
  uploads_data:

networks:
  dryfruto-network:
    driver: bridge
